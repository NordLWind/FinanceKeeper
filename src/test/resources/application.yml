spring:
  datasource:
    url: jdbc:h2:mem:test
    password: sa
    username: sa
    driver-class-name: "org.h2.Driver"
  jpa:
    hibernate:
      ddl-auto: update
  liquibase:
    change-log: db/changelog/changelog-testTransactionDao.xml